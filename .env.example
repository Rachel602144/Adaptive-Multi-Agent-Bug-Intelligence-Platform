# Copy to .env — NEVER commit .env
GOOGLE_API_KEY=your_own_gemini_key
GEMINI_MODEL=gemini-2.5-flash
# Leave empty to use local SQLite; set to Neon URL for production
DATABASE_URL=
FRONTEND_ORIGIN=*
PIPELINE_MODE=adaptive
